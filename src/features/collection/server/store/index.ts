import { atom } from "jotai";

export const isCollectionModalOpen = atom(false);

export const isDeleteCollectionModalOpen = atom(false);

export const isDeleteEntryModalOpen = atom(false);

export const isSheetOpen = atom(false);
